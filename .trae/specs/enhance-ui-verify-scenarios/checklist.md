- [x] `verify_ui` 支持至少一种新增业务级断言能力，并保持现有 `dom_change` 兼容
- [x] URL allowlist / 私网限制的校验结果能以结构化方式返回给调用方
- [x] 失败交互结果包含足够的留证信息，调用方无需依赖服务端日志才能理解失败原因
- [x] MCP 通道集成测试覆盖增强后的安全边界或断言失败场景
- [x] 真实浏览器集成测试覆盖增强后的成功路径
- [x] 受影响测试在本地可重复执行，行为与规格一致
